---
import './link.css'
const {
    url,
    isButtonLike,
    linkId
} = Astro.props;
---
<div class="link-wrapper">
    <a id={linkId} href={url} class={`link ${isButtonLike ? 'button' : ''}`}>
        <slot/>
    </a>
</div>